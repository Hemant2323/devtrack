# --- Web framework ---
fastapi==0.115.12          # the API framework: routing, validation, OpenAPI docs
uvicorn[standard]==0.34.2  # the ASGI server that actually runs FastAPI

# --- Configuration ---
pydantic-settings==2.9.1   # reads settings from .env into a typed class

# --- Database ---
sqlalchemy==2.0.41         # ORM: talk to the DB with Python classes, not raw SQL
alembic==1.16.1            # database migrations (versioned schema changes)
psycopg2-binary==2.9.10    # PostgreSQL driver. SQLite needs no driver, so this
                           # is only used in production; without it a
                           # postgresql:// URL fails at import with
                           # ModuleNotFoundError.

# --- Authentication ---
bcrypt==4.3.0              # password hashing (one-way, salted)
pyjwt==2.10.1              # create/verify JSON Web Tokens
email-validator==2.2.0     # lets Pydantic validate EmailStr fields

# --- Testing ---
pytest==8.3.5              # test runner
httpx==0.28.1              # HTTP client used by FastAPI's TestClient

# --- Linting ---
ruff==0.16.5               # lint gate; CI runs `ruff check app/ tests/`
